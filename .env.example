# Copie este arquivo para ".env" e ajuste os valores reais.
# NUNCA commitar o arquivo ".env" (já está no .gitignore).

POSTGRES_USER=postgres
POSTGRES_PASSWORD=troque_essa_senha
POSTGRES_DB=estoque_autopecas
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Caminho local da CÓPIA do banco legado (nunca o arquivo original em produção)
MDB_FILE_PATH=./data/cabrito_copia.accdb
